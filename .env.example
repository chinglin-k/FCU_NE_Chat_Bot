# .env.example — 環境變數範本
#
# 此檔案為範本說明，不含實際機密資訊，可安全提交至 Git
# 請勿將含有真實值的 .env 提交至 Git（已加入 .gitignore）
#
# 本專案的機密資訊儲存於 Google Apps Script Script Properties，
# 前端無需 .env 檔案。以下為說明用途：

# Gemini API Key（儲存於 GAS Script Properties，名稱：GEMINI_API_KEY）
# GEMINI_API_KEY=你的_Gemini_API_Key

# Google 試算表 ID（儲存於 GAS Script Properties，名稱：SPREADSHEET_ID）
# 請勿將真實 ID 提交至 Git，應由 GAS 端從 Script Properties 讀取
# SPREADSHEET_ID=你的_試算表_ID

# reCAPTCHA v3 Secret Key（儲存於 GAS Script Properties，名稱：RECAPTCHA_SECRET_KEY）
# 注意：這是 Secret Key，不是 Site Key；Site Key 為公開金鑰，設計上可安全寫在 js/config.js
# 請勿將真實 Secret Key 提交至 Git
# RECAPTCHA_SECRET_KEY=你的_reCAPTCHA_v3_Secret_Key

# GAS Web App URL（部署後填入 js/config.js 的 GAS_URL）
# GAS_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
